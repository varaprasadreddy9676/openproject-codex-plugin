# Security Policy

- Do not commit API tokens, secret files, or private instance URLs that should not be public.
- Use `OPENPROJECT_API_TOKEN_FILE` or `OPENPROJECT_BASIC_API_TOKEN_FILE` for local development.
- Report security issues privately to the repository owner instead of opening a public issue.
